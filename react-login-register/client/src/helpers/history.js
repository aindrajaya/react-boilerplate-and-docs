import { createBrowserHistory } from "history";

//to manage session history
export const history = createBrowserHistory();

//session hitory is a wayt to keeps tracks of the navigations in each apps, to support back/forward navigations and session restore.
//the History interface also allows manipulate of the browser session history, that is the pages visited in the tab or frame that the current page is loaded in.
